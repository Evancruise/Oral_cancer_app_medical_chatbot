<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>醫快拍App</title>
    <link rel="stylesheet" href="/css/loginpage.css">
    <link rel="stylesheet" href="/css/modal.css">
    <link rel="stylesheet" href="/css/homepage.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="main-container">
        <div class="login-form-professor <%= login_role === 'professor' ? '' : 'hidden' %>" id="professor">
            <div class="login-container">
                <form class="login-form" action="/api/auth/sign-in" id="professor-tab">
                    <div class="header">
                        <img src="/static/images/logo.png" alt="App Logo" class="logo" />
                    </div>
                    <input type="text" id="login_role" name="login_role" value="<%= login_role %>" hidden/>

                    <div class="form-group">
                        <label for="name">使用者姓名</label>
                        <input
                        type="text"
                        id="name"
                        name="name"
                        placeholder="姓名"
                        value="<%= typeof name !== 'undefined' ? name : '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="email">帳號 (Email)</label>
                        <input
                        type="text"
                        id="email"
                        name="email"
                        placeholder="帳號"
                        value="<%= typeof email !== 'undefined' ? email : '' %>"
                        />
                    </div>

                    <div class="form-group">
                        <label for="password">密碼</label>
                        <input type="password" id="password_professor" name="password" required />
                    </div>

                    <button type="submit" class="btn-primary">登入</button>
                
                </form>

                <a href="/api/auth/changepwd" class="link-forgot"> 忘記密碼？ </a>
                <a href="/api/auth/register" class="link-forgot"> 還沒有帳號? 註冊 </a>
                
                <!--
                <a href="/api/auth/changepwd" class="link-forgot">忘記密碼?</a>
                <a href="/api/auth/homepage" class="link-forgot"> 返回首頁 </a> -->

                <a href="/api/auth/homepage" class="link-forgot"> 返回首頁 </a>
                <div class="footer">
                    <a href="#" style="text-decoration:none; font-weight:800; color:#3aa3d1">Mi-tech</a> Copyright © 2019
                </div>
            </div>
        </div>
        
        <div class="login-form-patient <%= login_role === 'patient' ? '' : 'hidden' %>" id="patient">
            <form class="login-form" action="/api/auth/sign-in" id="patient-tab" hidden>

                <!-- 🔹 整體容器：左右排列 -->
                <div class="login-container">
                    <div class="header">
                        <img src="/static/images/logo.png" alt="App Logo" class="logo" />
                    </div>
                    <!-- ✅ 左側：快速登入 -->
                    <div class="login-left">
                        <h3 class="login-title">🔍 快速登入</h3>
                        <div class="qr-section">
                        <img
                            id="qr-image"
                            src="/api/auth/rebind-qr"
                            alt="QR Code"
                            class="qr-image"
                        />
                        <div id="countdown" class="countdown"></div>
                        <div id="scan-result" class="scan-result"></div>
                        <canvas id="snapshot" style="display: none; width: 300px; border: 1px solid #ccc;"></canvas>
                        </div>
                        <p class="hint-text">請使用行動裝置掃描 QR Code 以完成快速登入</p>
                    </div>

                    <!-- ✅ 右側：正常登入 -->
                    <div class="login-right">
                        <h3 class="login-title">🧾 正常登入</h3>

                        <input type="text" id="login_role" name="login_role" value="<%= login_role %>" hidden/>

                        <div class="form-group">
                        <label for="name">使用者姓名</label>
                        <input
                            type="text"
                            id="name_patient"
                            name="name"
                            placeholder="姓名"
                            value="<%= typeof name !== 'undefined' ? name : '' %>"
                        />
                        </div>

                        <div class="form-group">
                        <label for="email">帳號 (Email)</label>
                        <input
                            type="text"
                            id="email_patient"
                            name="email"
                            placeholder="帳號"
                            value="<%= typeof email !== 'undefined' ? email : '' %>"
                        />
                        </div>

                        <div class="form-group">
                        <label for="password">密碼</label>
                        <input type="password" id="password_patient" name="password" required />
                        </div>

                        <button type="submit" class="btn-primary full-btn">登入</button>

                        <div class="login-links">
                        <a href="/api/auth/changepwd" class="link-forgot">忘記密碼？</a>
                        <a href="/api/auth/register" class="link-forgot">註冊新帳號</a>
                        <a href="/api/auth/homepage" class="link-forgot">返回首頁</a>

                        <div class="footer">
                            <a href="#" style="text-decoration:none; font-weight:800; color:#3aa3d1">Mi-tech</a> Copyright © 2019
                        </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div id="modal-container"></div>
    <div id="modal-container-loading"></div>
    <div id="modal-container-2"></div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/jsQR.js"></script>
    <script type="module" src="/js/login.js"></script>
</body>
</html>